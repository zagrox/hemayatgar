# دستیار هوش مصنوعی سایت

یک دستیار چت داخل پنل مدیریت (`/admin/ai-assistant`، فقط برای نقش مدیر کل) که می‌تواند با
دستور فارسی شما محتوای سایت را تغییر دهد.

## قابلیت‌های فعلی

- نوشتن و انتشار مقاله جدید در وبلاگ، ویرایش یا حذف مقالات موجود
- ویرایش محتوای زیررشته‌های بیمه (معرفی، پوشش‌ها، استثناء، مزایا)
- ویرایش صفحات ثابت (درباره ما، تماس با ما، همکاری با ما)
- جایگزینی تصویر شاخص یک رشته بیمه یا کاور یک مقاله با عکسی که در چت پیوست می‌کنید
- افزودن یا حذف اسلاید در اسلایدر صفحه اصلی
- حذف تصویر از کتابخانه رسانه
- مشاهده و تغییر وضعیت درخواست‌های مشاوره، ثبت یادداشت داخلی روی هر درخواست
- مشاهده فهرست مشتریان و ثبت سابقه ارتباط (تماس/جلسه) با آن‌ها
- مشاهده و ویرایش تنظیمات سایت (اطلاعات تماس، شبکه‌های اجتماعی، عنوان و توضیحات پیش‌فرض سئو)
- **افزودن/ویرایش/حذف/جابه‌جایی یک بخش کامل به هر صفحه** (هیرو، متن+عکس، گرید ویژگی‌ها، نظرات
  مشتریان، نوار CTA، گالری تصویر، یا HTML سفارشی) — بدون نیاز به تغییر کد یا دیپلوی مجدد.
  مثلاً: «یه بخش نظرات مشتریان به صفحه اصلی اضافه کن» یا «یه بنر تبلیغاتی جدید بالای فوتر بذار».
  صفحات پشتیبانی‌شده: صفحه اصلی (`home`)، درباره ما/تماس/همکاری، هر رشته بیمه
  (`insurance-{slug}`)، و هر مقاله (`article-{slug}`).
- **دیدن واقعی طرح گرافیکی** — اگر عکسی از یک طراحی/صفحه بفرستی و بگی «این رو کد بزن و اضافه
  کن»، دستیار واقعاً تصویر را تحلیل می‌کند (نه فقط لینکش) و یک بخش HTML متناظر می‌سازد.
- **رنگ و فونت زنده کل سایت** — «رنگو سبز کن» یا «فونتو عوض کن»، بدون دیپلوی مجدد اعمال می‌شود.
- **ریسایز/کراپ حرفه‌ای خودکار تصویر** — هر عکسی که برای هر محل (لوگو، هیرو، آیکون رشته بیمه،
  کاور مقاله، اسلایدر) جایگزین می‌شود، خودکار با ابعاد دقیق همان قالب کراپ/ریسایز می‌شود (با
  کراپ هوشمند که جالب‌ترین بخش عکس مثل صورت را نگه می‌دارد)، دقیقاً مثل سایت‌سازهای حرفه‌ای.

### چیزی که عمداً اضافه نشده

- ساخت/حذف کاربر پنل و تغییر نقش‌ها و مجوزها — همیشه باید دستی از «مدیریت کاربران» انجام شود،
  چون یعنی دادن دسترسی ادمین به شخص دیگر و ریسک امنیتی دارد.
- **نوشتن و دیپلوی کد جدید** (کامپوننت React تازه، تغییر ساختار فنی، ادغام سرویس خارجی جدید).
  سیستم بخش‌های پویا (بالا) اکثر نیازهای «اضافه/کم کردن یک بخش» را بدون کد پوشش می‌دهد؛ ولی
  تغییرات واقعاً فنی (مثل افزودن یک صفحه با منطق کاملاً جدید یا اتصال یک API خارجی تازه)
  همچنان نیاز به یک توسعه‌دهنده یا Claude Code دارد — چون AI که مستقیم کد تولید و روی سرور
  زنده دیپلوی کند، بدون نظارت انسانی می‌تواند سایت را از کار بیندازد.

## فعال‌سازی

1. یک کلید API از [console.anthropic.com](https://console.anthropic.com) بگیرید
2. در فایل `.env` این خط را اضافه کنید:
   ```
   ANTHROPIC_API_KEY="sk-ant-..."
   ```
3. سرور API را ری‌استارت کنید

اگر این کلید تنظیم نشود، صفحه دستیار در پنل به‌جای چت، یک پیام راهنما نشان می‌دهد و هیچ
هزینه یا خطایی رخ نمی‌دهد.

## نوشتن خودکار هفتگی مقاله (اختیاری)

اگر می‌خواهید هر هفته یک مقاله جدید خودکار نوشته و منتشر شود، در `.env`:

```
ENABLE_AI_WEEKLY_ARTICLE="true"
AI_WEEKLY_ARTICLE_CRON="0 9 * * 6"   # پیش‌فرض: هر شنبه ساعت ۹ صبح
```

⚠️ توجه: این قابلیت پیش‌فرض **غیرفعال** است، چون هر بار اجرا هزینه API مصرف می‌کند و
سرور باید به‌صورت ۲۴ساعته روشن باشد تا زمان‌بند اجرا شود (روی Docker/سرور معمولی، نه روی
سرویس‌های serverless).

## هزینه

هر پیام چت یا هر مقاله خودکار، از حساب Anthropic شما (نه اشتراک Claude.ai) بر اساس مصرف واقعی
کسر می‌شود. هزینه هر مقاله معمولاً چند سنت است.

## محدودیت امنیتی

این دستیار فقط برای نقش **مدیر کل (SUPER_ADMIN)** در دسترس است، چون می‌تواند محتوای سایت را
مستقیماً تغییر دهد.
